# Rust build output (1.2 GB) — the crate is built from source.
/target/

# The web shell is a Flutter project; its build output and package config are
# regenerated by `flutter pub get` / `elpian run build`.
elpian_client/build/
elpian_client/.dart_tool/
elpian_client/.flutter-plugins-dependencies

# Scratch projects created by `elpian create` while testing the CLI.
/my-app/
