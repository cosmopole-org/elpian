import 'package:flutter/material.dart';
import '../models/stac_node.dart';

class HtmlMap {
  static Widget build(StacNode node, List<Widget> children) {
    // Image map - simplified representation
    return const SizedBox.shrink();
  }
}
