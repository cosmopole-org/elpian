import 'package:flutter/material.dart';
import '../models/elpian_node.dart';

class HtmlArea {
  static Widget build(ElpianNode node, List<Widget> children) {
    // Area is used inside map
    return const SizedBox.shrink();
  }
}
