import 'package:flutter/material.dart';
import '../models/elpian_node.dart';

class HtmlParam {
  static Widget build(ElpianNode node, List<Widget> children) {
    // Param is used inside object
    return const SizedBox.shrink();
  }
}
